{% extends 'base1.html.twig' %}

{% block head %}
    <base href="C:\wamp64\www\pharm\public\photos\">
{% endblock %}

{% block body %}
	<div class="container">
		<div class="col-md-12">
			<div class="card">
				<div class="card-header card-header-primary">
					<h2 class="card-title">Ajouter fiche</h4>
					<p class="card-category">Saisir les coordonnées du fiche</p>
				</div>
				<div class="card-body">
					{{ form_start(form) }}

					<div class="row">
						<div class="col-md-6">
							<div class="form-group bmd-form-group">
								<label class="bmd-label-floating">Quantité</label>
								{{ form_widget(form.qte, {'attr' : {'class' : 'form-control' }}) }}
							</div>
						</div>
						<div class="col-md-6">
							<div class="form-group bmd-form-group">
								<label class="bmd-label-floating">Prix de vente</label>
								{{ form_widget(form.prix_vente, {'attr' : {'class' : 'form-control' }}) }}
							</div>
						</div>
					</div>
					<button type="submit" class="btn btn-primary pull-right btn-round">Ajouter</button>
					<div class="clearfix"></div>
					{{ form_end(form) }}
				</div>
			</div>
		</div>
	</div>

	<a href="{{ path('medicament_index1') }}" class="btn btn-info btn-round">Retourner à liste</a>
{% endblock %}
